# Supabase Edge Function Environment Variables
# These are automatically configured when deploying to Supabase
# For local testing, copy this to .env and fill in your values

# Supabase URL (e.g., https://xxxxx.supabase.co)
SUPABASE_URL=your_supabase_project_url

# Supabase Service Role Key (secret key with full access)
# ⚠️ NEVER commit this key to git!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Supabase Anon Key (public key for client authentication)
SUPABASE_ANON_KEY=your_anon_key
